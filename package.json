{
  "name": "tangerine-invoicing",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"nodemon server.js\" \"npm run dev --prefix client\"",
    "start": "node server.js",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^24.40.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.0"
  }
}
